{
  "name": "page-deletion-guard-e2e-tests",
  "private": true,
  "scripts": {
    "test": "playwright test --reporter=list",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed --reporter=list"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0"
  }
}
